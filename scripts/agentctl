#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="$repo_root/orchestration${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m agentctl --root "$repo_root" "$@"

